import { Metadata } from "next";
import Register from "./page";  // import your Demo's page

export const metadata: Metadata = {
  title: 'Register - MyFapSheet',
  description:
    "Register for a new account",
};
export default Register;