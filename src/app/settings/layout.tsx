import { Metadata } from "next";
import Settings from "./page";  // import your Demo's page

export const metadata: Metadata = {
  title: 'Account Settings - MyFapSheet',
  description:
    "Account Settings for MyFapSheet",
};
export default Settings;